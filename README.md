# study-archive
A self-hosted pipeline that turns scanned notes and PDFs into a searchable, browsable study archive. Drop a file into a folder (or upload it through the browser), and it gets OCR'd, summarized, tagged, and rendered as a clean static page automatically, no manual data entry.
